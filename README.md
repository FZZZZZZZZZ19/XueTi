# 学缇 (XueTi)

一款简洁的**英语学习 App**。v2.06 版本包含：**严格依据教材原文开关 + 页码出处可点回 PDF 对照**、教材 PDF 手动划分章节页数 + 预览翻页 + 老书后补 PDF、可靠的每日提醒（精确闹钟 + 自检面板）、六级单词每日学习（完整词库 6662 词，数量可自定义，旧词自动回流）、小节内容全可编辑 + 自建例题 + 小节 AI 实时对话、精选题库、错题本、公式渲染（内置 KaTeX）、学习统计三大模块 + MPAndroidChart 图表、每日待办、六级学习合并页、AI 识图解题、学习日历与目标日倒计时，以及外观定制与应用内更新。

> 📦 **直接下载安装**：[GitHub Releases v2.06](https://github.com/FZZZZZZZZZ19/XueTi/releases/tag/v2.06)（`XueTi-v2.06-debug.apk`，Debug 签名，手机下载后直接安装）

## 功能特性

| 模块 | 说明 |
| --- | --- |
| 首页 | 今日学习进度卡片（已学 / 目标 + 进度条 + 状态提示）、功能入口宫格 |
| **每日单词**（v1.0 核心） | 卡片式学习：正面单词 + 音标，点击卡片翻转查看词性、释义、例句与翻译 |
| 认识 / 不认识 | 「认识」标记掌握；「不认识」自动排到本轮队尾再练一次（不重复计入今日进度），之后再答对按「连续答对 2 次」的复习规则处理 |
| 词库进度 | **每日序列 = 复习词（优先，约一半额度）+ 新词补足到每日目标**；今日目标已完成时进入纯复习模式（单轮最多 50 词） |
| **旧词回流**（v1.81） | 已学过的词不再一去不回：**标记过「不认识」的词**、以及**超过 14 天没练（可能遗忘）的词**会自动排进每日学习序列重温；学习页顶部显示「本轮：复习 x 词 + 新词 y 词」 |
| **每日待办**（v1.81） | 六级学习页新增「今日待办」卡片：**自动任务**（学习新词进度 / 复习巩固进度，点一下直接跳去学习或复习）+ **自定义待办**（自己写任务，勾选完成、长按删除、一键清除已完成）；跨天自动整理——已完成的清掉，未完成的顺延到第二天 |
| **自定义每日词数**（v1.80） | 设置 → 每日学习目标：**手动输入任意数量（1–500）**，并提供 10/20/30/50/100 快捷选择；学习日历时点某天还可「自定义数量…」单独设定当天要练多少词（0 = 休息日） |
| **独立复习**（v1.80 / v1.81） | 六级学习页显示「复习需复习的 N 个词」（含「不认识 x 个 · 超过 14 天没练 y 个」明细）→ 一键过一遍：卡片先只给单词回忆，点「想不起来」才显示释义与例句；**不认识的词连续答对 2 次才算掌握**、**很久没练的词答对一次即刷新计时**，答错则清零并排到本轮队尾；弹窗顶部显示「已掌握 x / N · 剩余 y 个」，可随时退出 |
| **我的词库** | 已学单词列表（含音标、释义、认识/需复习标签）与统计 |
| **词典**（v1.80） | 六级学习页内嵌**全部 6662 词**的词典列表，支持**页内搜索**（英文单词模糊匹配 / 中文释义 / 词性）；每词带「未学 / 需复习 / 已掌握」状态标签，点词弹出详情（音标、释义、例句与翻译），可一键 **AI 造句**、**英译中**、**标记需复习 / 标记已掌握** |
| **AI 翻译 / 造句**（v1.80） | 六级学习页输入中文或英文 →「中英互译」（按内容自动判断方向，只输出译文，多义最多列 3 个）或「AI 造句」（为单词生成 3 个六级难度例句 + 中文翻译 + 搭配提示）；结果弹窗可一键复制，并显示本次 token 用量 |
| 设置 | 每日学习目标自定义、**解题提示词**、清空学习记录、版本信息 |
| **学习统计三大模块**（v1.91） | 统计页用 **MPAndroidChart** 重做可视化：**① 六级学习**（近 14 天学习曲线图、掌握情况饼图、复习队列柱状图「不认识 / 超期未练」、认识率 · 连续打卡 · 待复习 · 词库覆盖率汇总）；**② 全书学习**（全书小节进度饼图、每本书「已学 / 未学」分组柱状图、逐书进度明细含是否有教材 PDF）；**③ 错题学习**（近 14 天新增错题柱状图、错题掌握情况饼图、学科分布柱状图、总数 · 未掌握 · 已掌握 · 平均复习次数） |
| **精选题库**（v1.92） | 全书学习的小节例题**自动拆成单道题**（兼容小标题 / 「题目」标记 / 编号列表 / 整段四种模型输出格式），**每道题右上角一个「+ 加入精选」**，点一下就把这道题收藏进精选题库，并记录**书目 → 章节 → 小节**归属；精选题库页面按**书本分组**展示（`《书名》· N 题`），可搜索（题目/章节/书名）、只看未掌握、点开看题干与解答（KaTeX 渲染）、标记已掌握、长按或详情内**移出**、一键清除已掌握；已收藏的题按钮显示「已加入 ✓」（同书同节同一题不会重复收藏）；入口在「全书学习」页顶部按钮与任一小节页菜单 |
| **小节内容全可编辑**（v2.00） | 小节里的**知识点 / 公式 / 例题都能自己增删改**：知识点与公式点「编辑」直接在弹窗改 Markdown + LaTeX；例题可**逐题编辑 / 删除 / 新增**；改动即时存本地，重进仍在；该节被手动编辑过后，点「重新生成」会先提示将被 AI 覆盖，避免误删自己的笔记 |
| **自建例题**（v2.00） | 「+ 添加例题」三种方式：**自己打字**、**拍照**、**相册选图**——后两者用视觉模型把图片里的题目**提取成文本**（只出题干、公式转 LaTeX，最多 3 张），提取结果落到弹窗由你确认修改；每道题的**解答可自己写做题步骤**，也可点「AI 生成答案」生成分步解答并写回该题（仍可继续编辑）；自建题带「· 自建」标记，同样可加入精选题库 |
| **小节 AI 对话**（v2.00） | 每个小节都有**实时对话**入口（小节页菜单 →「小节 AI 对话」）：**每次提问都会把当前小节的知识点 / 公式 / 例题作为上下文**（现场从本地最新内容读取，手动编辑过的也会带上），可追问「这步为什么能约掉」「知识点缺了 XX」；AI 被要求指出资料里具体的缺漏并给出**可直接粘贴补充的完整内容**；对话按小节分别保存、可清空，回答用 KaTeX 渲染并可一键复制 |
| **教材 PDF 接地生成**（v1.90 / v2.02 / v2.04 / v2.05 / v2.06） | 新建书本时可**上传教材 PDF**（本机抽取原文并保存原文件，不上传）。**页数划分手动驱动**：`PageMapActivity` 按「章 → 节」顺序填「起始页 - 结束页」，**只填一个边界就自动顺延**；可边划分边「预览 PDF」翻页确认，也能「尽量自动定位」后手改；**老书也能补 / 换 PDF**。生成时**只把该节那几页原文发给 AI**。**v2.06 两道保险**：① **严格模式开关**（设置 → AI 接口，默认开启）——只讲原文内容，原文没有的写「原文未提及」，**原文没例题就不自己编题**，被截断时不许猜后续；关闭后允许补充背景知识，但必须用「（补充）」标出；② **页码出处**——知识条目 / 公式 / 例题开头都标 `[p.12]`（跨页 `[p.12-14]`、补充内容 `[补充]`），**点一下直接打开 PDF 第 12 页对照原文**，是不是教材里的内容一眼可查 |
| **错题本**（v1.90） | **AI 解题**里点「这题我算错了」、**复习**单词时点「这词我记错了」，就把题目 + AI 解答自动记入错题本；首页与解题页都有入口；支持搜索、只看未掌握、补充错因、**AI 再讲一遍**（分析错因 + 分步解法）、标记/取消已掌握、清除已掌握；**生成教材目录与小节讲解时，未掌握的错题会自动作为「必须重点覆盖的易错点」推送给 AI**（按标题匹配相关错题） |
| **公式渲染**（v1.90） | 内置 **KaTeX + marked**（`assets/render/`，完全离线）：AI 输出的 Markdown + LaTeX 在 **AI 解题结果、全书学习三模块、错题本详情**统一渲染；行内 `$...$`、独立 `$$...$$`、`\begin{pmatrix}` / `cases` / `aligned` 等环境都能正确显示，长公式可横向滚动 |
| **提示词设置化**（v1.90） | 解题提示词从 AI 解题页移到 **设置 → AI 接口 → 解题提示词**（多行编辑 + 一键恢复默认）；默认提示词已重写：先给答案、分步讲解、指出易错点，并强制用 LaTeX 写公式 |
| **界面风格**（v1.2） | 5 套配色自选：默认紫 / 海洋蓝 / 森林绿 / 日落橙 / 樱花粉（含深色模式适配，切换立即生效） |
| **自定义背景图**（v1.2） | 从相册选择图片作为全局背景，可调「淡化程度」（0-95）保证文字可读，一键恢复默认背景 |
| **应用内更新**（v1.2） | 直连 GitHub 仓库 Release 检查新版本：显示版本号与更新说明 → 应用内下载 APK → 调起系统安装；支持启动时自动检查（每天一次，可关闭） |
| **AI 识图解题**（v1.3~v1.7） | 拍照 / 相册**多选** / 纯文字三种出题方式，**最多 6 张图片**一起提交（缩略图选择、单张裁剪、单张增强）；**图像增强**（灰度化 / 自动对比度 / **高斯锐化** / Otsu 二值化）；答案可一键复制；提交后显示本次 token 用量 |
| **全书学习**（v1.6/v1.7） | ① 输入**书名 / 出版社 / 版次**，或粘贴目录文本，或**拍照/相册多选上传目录页**（可拍多张跨页），由 AI 生成**全书目录**（大章 → 每章若干小章）；② 目录页浏览，已学小节带标记；③ 点小章 →「开始学习」→ AI 整理成 **知识点 / 公式 / 例题** 三模块分框展示；④ **语言风格可选**：直白易懂 / 严谨全面（可重新生成）；⑤ 结果本地缓存、可复制、可清除 |
| **API 设置与计费**（v1.7） | 在**设置 → AI 接口**统一填写 DeepSeek API Key 与模型（不再散落在各页面）；**用量计费**：累计/今日请求次数、输入输出 token 数、按可自定义单价估算费用，可一键重置；**峰谷提醒**：实时显示当前是峰时还是空闲时段（**周一至周五 9:00–12:00、14:00–18:00 为高峰，其余约 5 折**）与切换倒计时，并可开启「空闲时段开始」通知提醒 |
| **六级学习**（v1.6 起，合并页） | 把六级相关的全部功能收在一个页面：今日进度条与开始学习按钮、今日/连续/累计/认识率四项概览、**需复习词复习入口**、**AI 翻译/造句**、**词典（全览 + 搜索）**、已学单词列表（带认识/需复习标记） |
| **全书学习**（v1.6，新功能） | ① 输入**书名 / 出版社 / 版次**，或**拍照上传目录页**，由 AI 生成**全书目录**（大章 → 每章若干小章）；② 目录页可折叠浏览，已学小节带标记；③ 点进一个小章 → 点「开始学习」→ AI 把本节整理成 **知识点 / 公式 / 例题** 三个模块，分别显示在三个框中；④ **语言风格可选**：直白易懂 / 严谨全面（切换后可重新生成）；⑤ 生成结果本地缓存、可复制全文、可删除 |
| **全书学习**（v1.6，新功能） | ① 输入**书名 / 出版社 / 版次**，或**拍照上传目录页**，由 AI 生成**全书目录**（大章 → 每章若干小章）；② 目录页可折叠浏览，已学小节带标记；③ 点进一个小章 → 点「开始学习」→ AI 把本节整理成 **知识点 / 公式 / 例题** 三个模块，分别显示在三个框中；④ **语言风格可选**：直白易懂 / 严谨全面（切换后可重新生成）；⑤ 生成结果本地缓存、可复制全文、可删除 |
| **学习统计 + 日历**（v1.4/v1.5） | 今日已学 / 连续打卡 / 累计已学 / 认识率概览；近 7 天柱状图；**学习日历**（可翻月，标注每天学习数量、达标状态与**目标日**）；**点击日期可自定义该日学习量**（跟随默认 / 休息日 / 常用数量 / **自定义数量…**）；今日学习内容逐词列表 |
| **目标日 + 手写目标**（v1.5） | 在日历中选一天 → **写下你的目标或激励语句**（如"一次过六级！"）设为**目标日**；**首页最底部**每次打开都显示目标语句与**距离目标日天数倒计时**（今天/已过也分别提示）；首页点目标卡片可随时修改日期与内容 |
| **每日提醒**（v2.03 重做可靠性） | 通知栏每日提醒学习：自定义时间与**自定义提醒句子**，点通知直达学习页。**v2.03 解决"不打开 App 就收不到提醒"**：① 声明 `SCHEDULE_EXACT_ALARM` 并在有权限时改用 **`setAlarmClock` / `setExactAndAllowWhileIdle` 精确闹钟**（原来只用 inexact 的 `setAndAllowWhileIdle`，久不打开会被系统按 App Standby 分桶延后甚至丢弃）；② **一次滚动排定未来 7 天**，每次触发/每次打开 App 都会补满，少一次触发也不断链；③ 开机（含快速开机）、应用更新、**用户刚授予精确闹钟权限**时都会自动重排（`BootReceiver`）；④ 设置页新增**提醒自检区**：显示下次提醒时间、闹钟是否真的挂在系统里、通知权限、精确闹钟权限、电池优化状态、**上次实际触发时间**，并可直接点击修复；⑤ 排障入口会优先跳到国产 ROM 的**自启动管理页**（小米/华为/OPPO/vivo/三星/魅族），再退到应用信息页或电池优化列表 |
| **账户余额与用量**（v1.7/v1.71） | 设置 → AI 接口显示 **DeepSeek 账户余额**（点击刷新，含赠送/充值明细，余额不足会告警）；API 用量与计费：累计/今日请求数、输入输出 token、按可自定义单价估算费用；峰谷时段实时状态与「谷时开始」通知提醒 |
| 界面 | Material 3 风格，支持深色模式 |

## 内置词库（完整六级词汇）

- `app/src/main/assets/cet6_words.json` —— **6662 个单词**（完整六级大纲：六级词汇 + 四级基础词）
  - 数据来源：开源词库 [KyleBing/dict](https://github.com/KyleBing/dict) 的 6 本词书（CET6_1/2/3 + CET4_1/2/3）并集去重
  - 每词包含：单词、音标（英式优先）、词性、中文释义（最多 4 条）、英文例句、例句翻译
  - **6468 词带例句**（约 97%），无例句的词条自动隐藏例句区域
  - 按"六级核心词在前、四级基础词在后"的顺序排列，学习时优先遇到六级词
- 词库为纯 JSON，可继续扩充或替换为其他考试词库（保持 `word/phonetic/pos/meaning/example/exampleCn` 字段即可）

### 大词库性能处理

| 措施 | 说明 |
| --- | --- |
| 流式解析 | 用 `android.util.JsonReader` 逐条读取，不构建完整 JSON 树（比 org.json 更快、内存占用更低） |
| 异步加载 | 解析在 `Dispatchers.IO` 执行 + Mutex 保证只解析一次，主线程不阻塞 |
| 冷启动预载 | `Application.onCreate` 后台预加载词库，用户点进学习页时通常已就绪 |
| 加载态 | 极端情况下学习页显示"正在加载词库…"，避免白屏

## 构建 APK

### 已附带可安装 APK

`apk/XueTi-v2.06-debug.apk`（Debug 签名，直接安装到手机）

```bash
adb install -r apk/XueTi-v2.06-debug.apk
```

### 从源码构建

```bash
# 环境：JDK 17+、Android SDK（compileSdk 37 / build-tools 36.0.0）
./gradlew assembleDebug          # Windows: gradlew.bat assembleDebug
# 产物：app/build/outputs/apk/debug/app-debug.apk
```

用 Android Studio 打开项目根目录也可直接 Sync + Build。

## 项目结构

```
app/src/main/
├── assets/cet6_words.json               # 六级词库（6662 词）
├── assets/render/                       # 公式渲染：KaTeX + marked + 字体（离线，v1.90）
├── java/com/xueti/learn/
│   ├── App.kt                           # Application：词库/进度/设置 + 通知渠道 + 提醒恢复 + PDFBox 初始化
│   ├── MainActivity.kt                  # 首页（今日进度 + 功能入口 + 错题本入口 + 启动自动检查更新）
│   ├── StudyActivity.kt                 # 每日单词学习（复习词回流 + 新词，卡片翻转 / 认识度判断）
│   ├── StudyHubActivity.kt              # 六级学习（进度 + 统计 + 复习 + 待办 + AI 翻译/造句 + 词典 + 词库）
│   ├── MistakeActivity.kt               # 错题本（搜索 / 只看未掌握 / AI 再讲一遍 / 标记掌握，v1.90）
│   ├── CuratedActivity.kt               # 精选题库（按书目分组 / 搜索 / 标记掌握 / 移出，v1.92）
│   ├── SectionChatActivity.kt           # 小节 AI 实时对话（带本节知识点/公式/例题上下文的追问，v2.00）
│   ├── PdfPreviewActivity.kt            # 教材 PDF 预览（翻页/跳页/放大，标记起始页，v2.04）
│   ├── PageMapActivity.kt               # 手动划分章节页数（顺序联动自动顺延，老书可补 PDF，v2.05）
│   ├── TextbookListActivity.kt          # 全书学习：书本列表
│   ├── TextbookCreateActivity.kt        # 新建书本（书名/出版社/版次 或 拍照大纲 → AI 生成目录）
│   ├── TextbookDetailActivity.kt        # 书本目录（大章 → 小章）
│   ├── SectionStudyActivity.kt          # 小章学习（AI 三模块：知识点 / 公式 / 例题）
│   ├── WordListActivity.kt              # 我的词库（已学单词列表）
│   ├── AiSolveActivity.kt               # AI 识图解题（拍照 / 选图 / 文字 + 裁剪 + 增强 + 自定义提示词）
│   ├── CropActivity.kt                  # 图片裁剪（拖拽四角 + 旋转）
│   ├── StatsActivity.kt                 # 学习统计（概览 + 六级/全书/错题三模块图表 + 日历 + 今日内容）
│   ├── SettingsActivity.kt              # 设置中心（目标/提醒/外观/更新/数据）
│   ├── adapter/WordListAdapter.kt
│   ├── adapter/DictionaryAdapter.kt      # 词典列表（单词 + 音标 + 释义 + 未学/需复习/已掌握标签）
│   ├── ai/DeepSeekClient.kt             # DeepSeek 对话补全（文本 + 图片 base64）
│   ├── base/BaseActivity.kt             # 套用界面风格 + 自定义背景
│   ├── view/CropView.kt                 # 自绘裁剪视图
│   ├── data/
│   │   ├── WordRepository.kt            # 词库流式解析（JsonReader，异步 + 缓存）
│   │   ├── ProgressStore.kt             # 学习进度持久化
│   │   ├── SettingsStore.kt             # 应用设置（风格/背景/更新/提醒/AI Key）
│   │   ├── ToDoStore.kt                 # 每日待办（勾选/删除/跨天整理，未完成顺延）
│   │   ├── MistakeStore.kt              # 错题本数据结构（题目/解答/错因/来源/掌握状态 + 易错点推送，v1.90）
│   │   ├── PdfTextExtractor.kt          # 教材 PDF 逐页抽文本、目录摘要、小节原文定位（v1.90）
│   │   ├── CuratedStore.kt              # 精选题库（按书/章/节保存收藏的例题，去重 + 掌握状态，v1.92）
│   │   ├── SectionChatStore.kt          # 小节对话记录（按书 + 小节分文件保存，v2.00）
│   │   ├── PdfFileStore.kt              # PDF 原文件保存（filesDir/pdf_<bookId>.pdf，预览用，v2.04）
│   │   └── TextbookStore.kt             # 书本目录与小章生成内容（filesDir JSON）
│   ├── model/Word.kt                    # Word / Familiarity / LearnRecord
│   ├── model/Textbook.kt                # Textbook / Chapter / Section / SectionContent / StudyStyle
│   ├── update/
│   │   ├── UpdateChecker.kt             # 直连 GitHub Release 检查版本
│   │   ├── ApkDownloader.kt             # 下载 APK + FileProvider 安装
│   │   └── UpdateUi.kt                  # 更新弹窗 / 下载进度
│   ├── util/
│   │   ├── ThemeStyle.kt                # 5 套界面风格
│   │   ├── BackgroundHelper.kt          # 背景图解码（降采样）+ 蒙版
│   │   ├── ImageEnhancer.kt             # 图像增强（灰度/对比度/高斯锐化/Otsu 二值化）
│   │   ├── GoalEditor.kt                # 目标日 + 手写目标语句编辑弹窗
│   │   ├── GoalCountEditor.kt           # 每日练词数量编辑弹窗（自定义 1–500 + 常用数量）
│   │   ├── FormulaRenderer.kt           # 公式渲染（内置 KaTeX 的 WebView，可回传内容高度，v1.90）
│   │   ├── ExampleParser.kt             # 例题整段文本 → 单题拆分（4 种格式兼容，v1.92）
│   │   ├── PdfDocumentHandle.kt         # PDF 页面渲染（系统 PdfRenderer，按需渲染单页，v2.04）
│   │   ├── AiResultDialog.kt            # AI 结果弹窗（翻译/造句，可一键复制）
│   │   ├── NotificationHelper.kt        # 通知渠道
│   │   └── ReminderNotifier.kt          # 提醒通知构建（定时/测试共用）
│   └── work/
│       ├── ReminderScheduler.kt         # AlarmManager 每日闹钟调度
│       ├── ReminderReceiver.kt          # 闹钟触发 → 发通知 + 续期
│       └── BootReceiver.kt              # 开机/更新后恢复闹钟
└── res/                                 # 布局、5 套主题（含 values-night）、图标、字符串
```

## AI 识图解题使用说明

1. 到 [platform.deepseek.com](https://platform.deepseek.com/) 申请 API Key（APP **不内置**任何 Key，Key 只保存在本机）
2. 首页 →「AI 识图解题」→ 填写 API Key
3. 三种出题方式（可组合）：
   - **拍照**：直接拍题目照片
   - **相册**：选择已有题目图片
   - **文字**：手动输入题目（也可与图片同时提交）
4. **提示词由你自己写**（已内置一份默认提示词，可随意修改；会随题目一起发送）
5. 点「开始解题」→ 显示解答，可一键复制

> 默认模型 `deepseek-flash` 支持图片理解（OpenAI 兼容格式，图片以 base64 内联发送，最长边自动压缩到 1600px）。
> 也可在界面里改成其它模型名。图片使用限制参考 [DeepSeek 图像理解文档](https://api-docs.deepseek.com/zh-cn/guides/vision/)（支持 JPEG/PNG/GIF/WebP，单图 ≤32MiB）。

## 技术要点

- **Kotlin + Material 3 + ViewBinding**，minSdk 26 / targetSdk 34
- 构建：AGP 9.3.0（**使用内置 Kotlin，不应用 KGP 插件**）+ Gradle 9.7.1 + JDK 17
- 进度持久化：SharedPreferences 存 JSON；「今日已学」按日期自动归零（跨天重置）
- 词库：`JsonReader` 流式解析 1.4MB / 6662 词，IO 线程 + 单次缓存 + 冷启动预载
- 学习流程：队列制；不认识的词重新入队，`counted` 标记避免重复计入今日进度
- 主题：`values` + `values-night` 双套配色；图标全部为 vector drawable（无 PNG 资源）

## 后续规划（未实现）

- 学习统计页（每日曲线、记忆曲线）
- 单词发音（TTS）
- 艾宾浩斯间隔复习（当前为「需复习词连续答对 2 次掌握」的手动复习）
- 词库扩充 / 导入自定义词库

## 版本

| 版本 | 内容 |
| --- | --- |
| v1.0 | 基础 UI（首页 / 我的词库 / 设置）+ 六级单词每日学习功能（示例词库 103 词） |
| v1.1 | **完整六级词库 6662 词**（含四级基础，97% 带例句）；词库改为流式解析 + 异步加载 + 冷启动预载；无例句词条自动隐藏例句区 |
| v1.2 | **界面风格自选**（5 套配色，含深色适配）；**自定义背景图**（选图 + 淡化程度 + 恢复默认）；**应用内更新**（直连 GitHub Release 检查 → 下载 → 安装，支持启动自动检查）；**每日通知提醒**（自定义时间 + 进度提示 + 点击进学习页） |
| v1.3 | **AI 识图解题**（拍照/相册/文字 + 用户自备 DeepSeek Key 与自定义提示词，`deepseek-flash` 图片理解）；**提醒可靠性修复**：WorkManager 改为 AlarmManager + 开机自动恢复闹钟，新增「测试提醒」与后台运行排障入口；通知渠道升级为高重要性（横幅+提示音）；移除 WorkManager 依赖，权限更精简 |
| v1.4 | **图片裁剪与增强**：自绘裁剪视图（拖拽四角 + 旋转 90°）、图像增强（灰度化 / 自动对比度 / **高斯反锐化掩模** / Otsu 二值化），让题目照片文字更清晰；**学习统计**：今日/连续/累计/认识率概览 + 近 7 天柱状图 + 今日学习内容列表；**学习日历**：翻月浏览、标注每日学习量与达标状态、**点击日期自定义该日目标**（跟随默认 / 休息日 / 5·10·20·30·50 词），学习页与首页按日目标联动 |
| v1.5 | **目标日 + 手写目标语句**：日历选一天写下目标/激励语句即设为目标日（日历中标出目标日）；**首页最底部**每次打开显示目标语句与**距离天数倒计时**（今天 / 已过 N 天分别提示）；首页点目标卡片可直接改日期与内容，支持清除目标 |
| v1.5.1 | **修复裁剪页全黑**：`CropView.setBitmap()` 未主动计算图片显示区域（设置图片不改变 View 尺寸，`onSizeChanged` 不回调），导致裁剪区空白；现在设置/旋转后立即重新适配，并在 `onDraw` 增加兜底。**修复预览看不清全图**：AI 解题页预览由 `centerCrop`（裁切放大）改为 `fitCenter`（完整显示全图）并加高到 240dp；**新增点击图片全屏查看**细节 |
| v1.6 | **六级学习合并页**：今日进度 + 统计概览 + 我的词库合并为一个页面（首页原「今日进度卡」与「我的词库」入口整合进来）。**全书学习（新）**：输入书名/出版社/版次或拍照上传目录页 → AI 生成**全书目录**（大章→小章）→ 点小章「开始学习」→ AI 输出**知识点 / 公式 / 例题**三模块并分框展示；**语言风格可选**（直白易懂 / 严谨全面），生成结果本地缓存、可复制、可清除 |
| v1.7 | **多图上传**：AI 解题与全书目录都支持**最多 6 张**图片（相册多选 + 缩略图选择，可单张裁剪/增强）；**API 设置集中到设置页**（Key + 模型 + 保存按钮，各页面只显示接口状态）；**用量与计费**：累计/今日请求数、输入输出 token、按可自定义单价估算费用、一键重置；**峰谷提醒**：实时峰/谷状态与切换倒计时，可开启每天 00:30「谷时开始」通知（AlarmManager 调度） |
| v1.71 | **自定义提醒句子**：设置 → 每日提醒 可写下自己的提醒语（如「该背单词啦，坚持就是胜利」），通知中显示该句子 + 今日进度，留空则只显示进度；**账户余额显示**：设置 → AI 接口新增「账户余额」，点击或进入页面自动查询 `GET /user/balance`，显示总额与赠送/充值明细，余额不足时告警 |
| v1.72 | **修正高峰/空闲时段（重要）**：按官方最新定价页，**高峰时段 = 北京时间周一至周五 9:00-12:00、14:00-18:00，其余全部为空闲时段（约 5 折）**；相应更新状态文案、下次切换倒计时、「空闲时段开始提醒」（改为在最近一个高峰结束点 12:00 / 18:00 通知）与计费说明 |
| v1.73 | **模型可自选**：设置 → AI 接口 的模型改为**下拉选择**（也可手动输入），点击「刷新可用模型」调用 `GET /models` 拉取账号可用模型并缓存；选择模型时**自动套用该模型的高峰默认单价**（flash 输入¥2/输出¥8、v4-pro 输入¥9/输出¥27），并提示 flash 支持图片理解、v4-pro 仅文本 |
| v1.80 | **（即 v1.8；上一版是 1.73，版本号按段号递增必须写 1.80，写成 1.8 会导致老版本 App 认为 8 < 73 而检测不到更新）** **每日练词数量自定义**：设置 → 每日学习目标改为**手输任意数量 1–500**（含 10/20/30/50/100 快捷键），学习日历增加「自定义数量…」。**独立复习**：六级学习页显示「复习需复习的 N 个词」，一键过一遍所有需复习词——先只给单词回忆，点「想不起来」才显示释义例句；**连续答对 2 次才算掌握并自动摘掉标记**，答错清零并排到队尾，顶部显示「已掌握 x / N · 剩余 y 个」。**词典**：六级学习页内嵌 6662 词全览 + **页内搜索**（英文模糊 / 中文释义 / 词性），每词带未学·需复习·已掌握标签，详情弹窗可 **AI 造句 / 英译中 / 标记需复习或已掌握**。**AI 翻译 / 造句**：输入中英文即可**中英互译**（自动判断方向）或 **AI 造句**（3 个六级例句 + 翻译 + 搭配提示），结果可一键复制并显示 token 用量。以上功能**全部集中在「六级学习」页**，不新增页面 |
| v1.81 | **旧词回流每日序列**：每日学习不再只学新词——**标记过「不认识」的词**与**超过 14 天没练（可能遗忘）的词**会优先排进当天序列（约占一半额度，其余给新词；新词不够时用复习词补满），学习页顶部显示「本轮：复习 x 词 + 新词 y 词」；今日目标完成后进入纯复习模式（单轮最多 50 词，不再影响今日进度）。**遗忘判定**：`LearnRecord` 新增 `lastSeenAt`，每次练习/复习都会刷新；「认识但超期」的旧词答对一次即重新计时，「不认识」的词仍需连续答对 2 次。**复习规则按类型区分**：复习卡片会提示当前词是「不认识」还是「很久没练」。**每日待办**：六级学习页新增待办卡片——自动任务（学习新词进度、复习巩固进度，点击直达）+ 自定义待办（勾选完成 / 长按删除 / 一键清除已完成 / 跨天未完成顺延），顶部显示「已完成 x / y」 |
| v1.90 | **教材 PDF 接地生成（重大）**：新建书本新增「上传教材 PDF」，本机用 PDFBox（`PDFTextExtractor`）逐页抽取原文并缓存（`filesDir/pdftext_<bookId>.json`，不联网、不上传）；生成目录时优先用 PDF 的**目录页原文摘要**，生成小节讲解时**按小节标题定位原文片段**，提示词明确要求「只使用原文，原文没有的不要添加」，从根上压制幻觉；扫描版 PDF 会提示改用拍照目录页。**错题本（新）**：`MistakeStore` 数据结构（题目 / AI 解答 / 错因 / 来源 / 学科 / 掌握状态 / 复习次数）+ `MistakeActivity` 列表页（搜索、只看未掌握、清除已掌握）；入口在首页卡片与 AI 解题页；**AI 解题加「这题我算错了」、复习卡片加「这词我记错了」**，自动记录并可选补充错因；详情页可 **AI 再讲一遍**（错因分析 + 分步解法）、标记掌握、删除；**生成目录 / 讲解时自动把未掌握错题作为「必须重点覆盖的易错点」推送**（按标题匹配相关条目），状态栏显示「已结合错题本重点」。**公式渲染（新）**：内置 KaTeX + marked（`assets/render/`，离线 600KB），实现 `FormulaRenderer`（WebView + 可回传内容高度），AI 解题结果、全书学习三模块、错题本详情全部改为渲染视图，行内 / 独立 / 矩阵 / 分段函数等 LaTeX 均正确显示，长公式横向滚动；三个模块卡片按渲染高度自适应。**解题提示词设置化**：从 AI 解题页移到 设置 → AI 接口（多行编辑 + 恢复默认），默认提示词重写（先给答案 → 分步讲解 → 指出易错点 → 强制 LaTeX），AI 解题页只显示入口；解题请求统一追加排版要求。APK 体积因内置 PDF 解析与渲染引擎增至约 17 MB |
| v1.91 | **学习统计界面优化 + 图表可视化（MPAndroidChart v3.1.0，JitPack）**：统计页重做，除原有「今日/连续/累计/认识率」概览、学习日历、今日学习内容外，新增三大模块——**① 六级学习**：近 14 天每日学习**折线图**（曲线 + 渐变填充）、掌握情况**饼图**（中心显示认识率）、复习队列**分组柱状图**（不认识 / 超期未练），并有「认识率 · 连续打卡 · 待复习 · 词库覆盖（已学/6662 词）· 今日目标」文字汇总；**② 全书学习**：全书小节进度**饼图**（已学/未学，中心显示百分比）、每本书「已学/未学小节」**分组柱状图**（最多 5 本，书名自动截断）、逐书明细（`《书名》 3/12 节 · 25% · 有教材 PDF`），无书时给出引导文案；**③ 错题学习**：近 14 天新增错题**柱状图**、错题掌握情况**饼图**、学科分布**柱状图**（前 6 名）、汇总（总数 · 未掌握 · 已掌握 · 平均复习次数）；所有图表配色自动跟随深浅色主题与 5 套界面风格，含空数据提示与坐标轴范围处理（分组柱不被裁切） |
| v1.92 | **全书学习 · 精选题库（新）**：小节例题从「一大段文本」变成**一道题一张卡片**——`ExampleParser` 兼容四种模型输出格式（`### 例题 1` 小标题 / `**题目**` 标记 / `1.` 编号列表 / 无标记整段），自动把例题拆成单题并分离「题干 / 解答」；同时把 AI 提示词改成**结构化输出**（`examples: [{title, question, solution}]`），新旧缓存都能用。每道例题卡片右上角有 **「+ 加入精选」**，点一下即收藏（已收藏显示「已加入 ✓」并可看到「已收藏 x / y 道」进度），同书同节同一题不会重复收藏。新增**精选题库页** `CuratedActivity`：按**书本分组**（`《书名》· N 题`）→ 题目行显示 `章节 › 小节` 路径与题干摘要，支持搜索（题目 / 解答 / 章节 / 书名）、只看未掌握、详情用 KaTeX 渲染题干与解答、标记已掌握、移出（长按或详情内）、一键清除已掌握；入口在「全书学习」页顶部按钮与任一小节页菜单。数据保存在 `filesDir/curated.json`（`CuratedStore`）。附带 `tools/check_example_parser.js` 离线校验四种格式的拆分结果 |
| v2.00 | **全书学习大版本：内容可编辑 + 自建例题 + 小节实时对话**。**① 内容全可编辑**：知识点 / 公式点「编辑」在弹窗里改（Markdown + LaTeX），例题逐题「编辑 / 删除 / 新增」，改动即时存本地（`SectionContent.customized` 标记手动编辑），被编辑过的小节点「重新生成」会先确认再覆盖。**② 自建例题**：「+ 添加例题」＝ 自己打字 / 拍照 / 相册选图；拍照或选图后用视觉模型（最多 3 张）把题目**提取成文本**（只出题干、公式转 LaTeX）再落进编辑弹窗；解答可自己写做题步骤，也可「AI 生成答案」按大题风格生成分步解答写回该题；自建题带 `userAdded` 标记与「· 自建」标签，可继续加入精选题库。**③ 小节 AI 实时对话**（`SectionChatActivity`）：每次发送都**重新读取本地最新小节内容**并把知识点 / 公式 / 例题作为上下文，叠加最近 12 轮对话；提示词要求 AI 指出资料里**具体哪一条**缺失并给出可直接粘贴的补充内容；对话按小节存 `chat_<hash>.json`，可清空，AI 回答用 KaTeX 渲染并可复制。同时修复 `FormulaRenderer.attach()` 在 WebView 复用时误判「已就绪」导致内容丢失的问题 |
| v2.01 | **修复公式不渲染（重要）**：模型用 `\(...\)`、`\[...\]` 写公式时，Markdown 解析器（CommonMark）会把 `\(`、`\]`、`\_`、`\{` 这些反斜杠序列当成**转义符吃掉**，KaTeX 只能看到裸的 `\cap`、`\bar`，于是整段公式按纯文本显示（截图里"例题 3 用事件运算表示文字事件"就是这个现象）。现在渲染前先把公式整段**保护成占位符**（`$$...$$`、`\\[...\\]`、`\\(...\\)`、`$...$`、裸 `\begin{}` 环境全覆盖），解析完 Markdown 再原样放回交给 KaTeX，因此 `\(A\cap\bar B=A-B\)` 这类写法也能正常渲染；该修复对所有渲染位置生效（AI 解题、小节三模块、例题、错题本、精选题库、小节对话）。附带 `tools/check_math_render.js` 离线回归测试：直接加载 app 内真实的 `render.html` 脚本跑 7 种公式写法 + 截图 bug 回归，全部通过 |
| v2.02 | **修复 AI 解题答案显示不全 + 解决几百页教材 PDF 的页码映射（重要）**。**① 答案显示不全**：解答 WebView 原来固定 360dp 又嵌在外层 ScrollView 里，既不能内部滚动又被裁掉；现在改为**按渲染后的真实高度自适应**（先渲染、再量 `contentHeight()`、350ms 后再补量一次，上限 12000dp），长解答由外层统一滚动。**② 几百页 PDF**：新增 `PdfOutlineParser` **本地解析 PDF 目录**——识别「标题 …… 页码」点线式与空白表格型目录、支持跨页目录、按编号与「第 X 章」判定章/节层级、并自动校正**印刷页码 → PDF 物理页码**的偏移（用正文标题出现位置投票取众数）；解析成功就**直接生成目录 + 每节页范围，完全不花 token**。解析不出来时退回 AI 生成目录，再用 `PdfTextExtractor.pageIndexDigest()` 生成**页码索引**（每页「第 N 页 | 开头文字」，几百页也只有 2~3 万字符）交给 `DeepSeekClient.mapSectionsToPages()` 让模型给每节定起始页，本地标题定位（跳过目录页）优先、AI 兜底，结束页 = 下一节起始 - 1。讲解时只把**该节对应页范围**的原文发给模型（`pagesInRange`，带页码标注、超长截断），彻底避免"拿目录页当正文"；小节页顶部显示「教材原文：p.120–135（约 16 页）· 点击可修改」，可手动改页范围或点「自动定位」，映射结果存进 `Textbook.pageMap`。附 `gradlew tocSelfCheck` 离线自检（18 项断言，直接跑真实 Kotlin 解析逻辑）与 JUnit 测试 `PdfOutlineParserTest` |
| v2.03 | **修复「只有 App 在后台时才收到提醒」**。原因有三层：① 原来只用 `setAndAllowWhileIdle`（**非精确**闹钟），App 久不打开会被系统划入 App Standby 的「限制/极少使用」分桶，闹钟被大幅延后甚至直接丢弃；② Android 12+ 没有「闹钟与提醒」精确闹钟权限，用不了精确定时；③ 只排下一个闹钟，一次没触发就断链。修复：**声明 `SCHEDULE_EXACT_ALARM`**，有权限时用 `setAlarmClock`（最近一次，系统按用户可见闹钟处理）与 `setExactAndAllowWhileIdle`（后续）排定；**一次滚动排未来 7 天**，触发后与每次打开 App 都补满；`BootReceiver` 增加快速开机、`SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED`（用户刚授权时立刻重排）并改为 `exported=true`；新增 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 与一键申请忽略电池优化；设置页提醒区新增**自检面板**（下次提醒、闹钟是否真挂在系统、通知权限、精确闹钟、电池优化、**上次实际触发时间**、`重新排定` 按钮）以及**国产 ROM 自启动管理页直达**（小米/华为/OPPO/vivo/三星/魅族，失败则退到应用信息页）。同时在设置页与自检文案里说明：刚安装或「强制停止」后系统会冻结闹钟，需手动打开一次 App |
| v2.04 | **教材 PDF 预览：自己翻页定位页码**。自动定位在扫描版/复杂版式上经常找不到（正因如此才要手动填起始页），但之前 App 里连 PDF 都看不了。现在：① 导入 PDF 时会把**原文件**也保存到 `filesDir/pdf_<bookId>.pdf`（`PdfFileStore`，删书时一并清理），旧书只有文字、预览会提示重新上传；② 新增 `PdfPreviewActivity`：用系统 `PdfRenderer` 按需渲染单页（`PdfDocumentHandle`，IO 线程、白底、最大 2400px），支持**上一页/下一页、输入页号跳转、页码角标、放大/适应宽度**（放大后横向纵向都能滚）；③ 底部「**设为起始页**」「**设为结束页**」——翻到小节开头点一下，返回时页号自动带回：从**小节页菜单 →「教材 PDF 预览」**进入会直接保存该节页范围，从**页范围弹窗 →「预览 PDF 选页」**进入则回填到输入框；只标了一头时，另一头按弹窗已填值或 +8 页估算；④ 书本目录页的溢出菜单也加了「教材 PDF 预览」，可从第 1 页浏览全书 |
| v2.05 | **改为「用户手动划分章节页数」**（自动目录提取在扫描版/复杂版式上确实不可靠）。新增 `PageMapActivity`（入口：**书本目录页菜单 →「划分章节页数」**）：① 按「章 → 节」顺序列出该节标题与两个输入框（起始页 / 结束页），已保存的会回填；② **顺序联动**——填某节的**结束页**，下一节**起始页自动 = 结束页 + 1**，并按上一节长度给出建议结束页；反过来填某节**起始页**，上一节**结束页自动 = 起始页 − 1**（用标志位抑制 watcher 递归）；③ 每行有「预览」按钮、顶部有「教材 PDF 预览」，翻页看准了再填；④「尽量自动定位」（本地标题定位，跳过目录页）与「清空」；⑤ 保存写入 `Textbook.pageMap`，讲解时**只发该节那几页**给 AI。**老书后补 PDF**：书本目录页菜单新增「添加 / 更换教材 PDF」（`PdfBookImporter`：抽文字 + 保存原文件 + 更新书目信息），导入后弹窗询问是否立刻划分页数；建书时若带 PDF 也会在创建后直接叠一层划分页；新增 `TextbookStore.replacePageRanges()` 支持真正清空 |
| v2.06 | **回答"加了 PDF 是不是就严格按教材生成"**：之前只是提示词里的强约束，仍有 5 个漏点（例题模块自相矛盾会编题、原文超 12000 字符被截断且模型不知道、错题本注入属额外内容、页码填错、扫描版无文字层直接退化为无接地）。本版按选择补两道保险：① **严格模式开关**（设置 → AI 接口 →「严格依据教材原文」，默认开启）——`SettingsStore.strictGrounding` 传给 `generateSectionContent(strict=…)` 与 `askAboutSection(strict=…)`：开启时提示词分 4 条硬规则（只用原文 / 信息不足写「原文未提及」/ 原文没例题就写「原文未举例题」不自己编 / 被截断不许猜后续），关闭时改为"以原文为主 + 补充内容必须标「（补充）」"；小节状态栏会显示「严格依据原文 / 允许补充知识」。② **页码出处可点回原文**：提示词要求每条知识、公式、例题开头标 `[p.12]`（跨页 `[p.12-14]`、补充标 `[补充]`），`render.html` 里 `linkifyPageRefs()` 把 `[p.N]` 变成 `href="xueti-page:N"` 链接（带浅蓝底样式），`FormulaRenderer.setPageClickHandler()` 通过 `shouldOverrideUrlLoading` 拦截并回调，小节页据此**直接打开 PDF 第 N 页对照**（知识点 / 公式 / 例题卡片都能点）。`tools/check_math_render.js` 增加 3 项离线断言（单页、跨页、与公式共存），全部通过 |
