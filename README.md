# 学缇 (XueTi)

一款简洁的**英语学习 App**。v1.80 版本包含：六级单词每日学习（完整词库 6662 词，**数量可自定义**）、**六级学习合并页（复习 + 词典 + AI 翻译/造句 + 统计 + 词库）**、**需复习词的独立复习（连续答对 2 次才算掌握）**、**词典全览与页内搜索**、**AI 中英互译 / 造句**、**全书学习（AI 生成教材目录 + 逐节三模块讲解）**、**AI 识图解题**、学习统计与日历、目标日倒计时，以及外观定制、应用内更新与每日提醒。

> 📦 **直接下载安装**：[GitHub Releases v1.80](https://github.com/FZZZZZZZZZ19/XueTi/releases/tag/v1.80)（`XueTi-v1.80-debug.apk`，Debug 签名，手机下载后直接安装）

## 功能特性

| 模块 | 说明 |
| --- | --- |
| 首页 | 今日学习进度卡片（已学 / 目标 + 进度条 + 状态提示）、功能入口宫格 |
| **每日单词**（v1.0 核心） | 卡片式学习：正面单词 + 音标，点击卡片翻转查看词性、释义、例句与翻译 |
| 认识 / 不认识 | 「认识」标记掌握；「不认识」自动排到本轮队尾再练一次（不重复计入今日进度） |
| 词库进度 | 按每日目标从未学词中取词，学完整轮显示完成页 |
| **自定义每日词数**（v1.80） | 设置 → 每日学习目标：**手动输入任意数量（1–500）**，并提供 10/20/30/50/100 快捷选择；学习日历时点某天还可「自定义数量…」单独设定当天要练多少词（0 = 休息日） |
| **独立复习**（v1.80） | 六级学习页显示「复习需复习的 N 个词」→ 一键过一遍所有**需复习**词：卡片先只给单词回忆，点「想不起来」才显示释义与例句；**连续答对 2 次才算掌握**并自动摘掉「需复习」标记，答错/想不起来则清零并排到本轮队尾再练；弹窗顶部显示「已掌握 x / N · 剩余 y 个」，可随时退出 |
| **我的词库** | 已学单词列表（含音标、释义、认识/需复习标签）与统计 |
| **词典**（v1.80） | 六级学习页内嵌**全部 6662 词**的词典列表，支持**页内搜索**（英文单词模糊匹配 / 中文释义 / 词性）；每词带「未学 / 需复习 / 已掌握」状态标签，点词弹出详情（音标、释义、例句与翻译），可一键 **AI 造句**、**英译中**、**标记需复习 / 标记已掌握** |
| **AI 翻译 / 造句**（v1.80） | 六级学习页输入中文或英文 →「中英互译」（按内容自动判断方向，只输出译文，多义最多列 3 个）或「AI 造句」（为单词生成 3 个六级难度例句 + 中文翻译 + 搭配提示）；结果弹窗可一键复制，并显示本次 token 用量 |
| 设置 | 每日学习目标自定义、清空学习记录、版本信息 |
| **界面风格**（v1.2） | 5 套配色自选：默认紫 / 海洋蓝 / 森林绿 / 日落橙 / 樱花粉（含深色模式适配，切换立即生效） |
| **自定义背景图**（v1.2） | 从相册选择图片作为全局背景，可调「淡化程度」（0-95）保证文字可读，一键恢复默认背景 |
| **应用内更新**（v1.2） | 直连 GitHub 仓库 Release 检查新版本：显示版本号与更新说明 → 应用内下载 APK → 调起系统安装；支持启动时自动检查（每天一次，可关闭） |
| **AI 识图解题**（v1.3~v1.7） | 拍照 / 相册**多选** / 纯文字三种出题方式，**最多 6 张图片**一起提交（缩略图选择、单张裁剪、单张增强）；**图像增强**（灰度化 / 自动对比度 / **高斯锐化** / Otsu 二值化）；答案可一键复制；提交后显示本次 token 用量 |
| **全书学习**（v1.6/v1.7） | ① 输入**书名 / 出版社 / 版次**，或粘贴目录文本，或**拍照/相册多选上传目录页**（可拍多张跨页），由 AI 生成**全书目录**（大章 → 每章若干小章）；② 目录页浏览，已学小节带标记；③ 点小章 →「开始学习」→ AI 整理成 **知识点 / 公式 / 例题** 三模块分框展示；④ **语言风格可选**：直白易懂 / 严谨全面（可重新生成）；⑤ 结果本地缓存、可复制、可清除 |
| **API 设置与计费**（v1.7） | 在**设置 → AI 接口**统一填写 DeepSeek API Key 与模型（不再散落在各页面）；**用量计费**：累计/今日请求次数、输入输出 token 数、按可自定义单价估算费用，可一键重置；**峰谷提醒**：实时显示当前是峰时还是空闲时段（**周一至周五 9:00–12:00、14:00–18:00 为高峰，其余约 5 折**）与切换倒计时，并可开启「空闲时段开始」通知提醒 |
| **六级学习**（v1.6 起，合并页） | 把六级相关的全部功能收在一个页面：今日进度条与开始学习按钮、今日/连续/累计/认识率四项概览、**需复习词复习入口**、**AI 翻译/造句**、**词典（全览 + 搜索）**、已学单词列表（带认识/需复习标记） |
| **全书学习**（v1.6，新功能） | ① 输入**书名 / 出版社 / 版次**，或**拍照上传目录页**，由 AI 生成**全书目录**（大章 → 每章若干小章）；② 目录页可折叠浏览，已学小节带标记；③ 点进一个小章 → 点「开始学习」→ AI 把本节整理成 **知识点 / 公式 / 例题** 三个模块，分别显示在三个框中；④ **语言风格可选**：直白易懂 / 严谨全面（切换后可重新生成）；⑤ 生成结果本地缓存、可复制全文、可删除 |
| **全书学习**（v1.6，新功能） | ① 输入**书名 / 出版社 / 版次**，或**拍照上传目录页**，由 AI 生成**全书目录**（大章 → 每章若干小章）；② 目录页可折叠浏览，已学小节带标记；③ 点进一个小章 → 点「开始学习」→ AI 把本节整理成 **知识点 / 公式 / 例题** 三个模块，分别显示在三个框中；④ **语言风格可选**：直白易懂 / 严谨全面（切换后可重新生成）；⑤ 生成结果本地缓存、可复制全文、可删除 |
| **学习统计 + 日历**（v1.4/v1.5） | 今日已学 / 连续打卡 / 累计已学 / 认识率概览；近 7 天柱状图；**学习日历**（可翻月，标注每天学习数量、达标状态与**目标日**）；**点击日期可自定义该日学习量**（跟随默认 / 休息日 / 常用数量 / **自定义数量…**）；今日学习内容逐词列表 |
| **目标日 + 手写目标**（v1.5） | 在日历中选一天 → **写下你的目标或激励语句**（如"一次过六级！"）设为**目标日**；**首页最底部**每次打开都显示目标语句与**距离目标日天数倒计时**（今天/已过也分别提示）；首页点目标卡片可随时修改日期与内容 |
| **每日提醒** | 通知栏每日提醒学习：自定义提醒时间与**自定义提醒句子**（留空则显示今日进度），通知内容显示今日进度与剩余词数，点击直达学习页；v1.3 改用 AlarmManager + 开机自动恢复（国产 ROM 后台清理下更可靠），并提供**测试提醒**与后台运行排障入口 |
| **账户余额与用量**（v1.7/v1.71） | 设置 → AI 接口显示 **DeepSeek 账户余额**（点击刷新，含赠送/充值明细，余额不足会告警）；API 用量与计费：累计/今日请求数、输入输出 token、按可自定义单价估算费用；峰谷时段实时状态与「谷时开始」通知提醒 |
| 界面 | Material 3 风格，支持深色模式 |

## 内置词库（完整六级词汇）

- `app/src/main/assets/cet6_words.json` —— **6662 个单词**（完整六级大纲：六级词汇 + 四级基础词）
  - 数据来源：开源词库 [KyleBing/dict](https://github.com/KyleBing/dict) 的 6 本词书（CET6_1/2/3 + CET4_1/2/3）并集去重
  - 每词包含：单词、音标（英式优先）、词性、中文释义（最多 4 条）、英文例句、例句翻译
  - **6468 词带例句**（约 97%），无例句的词条自动隐藏例句区域
  - 按"六级核心词在前、四级基础词在后"的顺序排列，学习时优先遇到六级词
- 词库为纯 JSON，可继续扩充或替换为其他考试词库（保持 `word/phonetic/pos/meaning/example/exampleCn` 字段即可）

### 大词库性能处理

| 措施 | 说明 |
| --- | --- |
| 流式解析 | 用 `android.util.JsonReader` 逐条读取，不构建完整 JSON 树（比 org.json 更快、内存占用更低） |
| 异步加载 | 解析在 `Dispatchers.IO` 执行 + Mutex 保证只解析一次，主线程不阻塞 |
| 冷启动预载 | `Application.onCreate` 后台预加载词库，用户点进学习页时通常已就绪 |
| 加载态 | 极端情况下学习页显示"正在加载词库…"，避免白屏

## 构建 APK

### 已附带可安装 APK

`apk/XueTi-v1.80-debug.apk`（Debug 签名，直接安装到手机）

```bash
adb install -r apk/XueTi-v1.80-debug.apk
```

### 从源码构建

```bash
# 环境：JDK 17+、Android SDK（compileSdk 37 / build-tools 36.0.0）
./gradlew assembleDebug          # Windows: gradlew.bat assembleDebug
# 产物：app/build/outputs/apk/debug/app-debug.apk
```

用 Android Studio 打开项目根目录也可直接 Sync + Build。

## 项目结构

```
app/src/main/
├── assets/cet6_words.json               # 六级词库（6662 词）
├── java/com/xueti/learn/
│   ├── App.kt                           # Application：词库/进度/设置 + 通知渠道 + 提醒恢复
│   ├── MainActivity.kt                  # 首页（今日进度 + 功能入口 + 启动自动检查更新）
│   ├── StudyActivity.kt                 # 每日单词学习（卡片翻转 / 认识度判断 / 重新排队）
│   ├── StudyHubActivity.kt              # 六级学习（进度 + 统计 + 需复习词复习 + AI 翻译/造句 + 词典 + 我的词库）
│   ├── TextbookListActivity.kt          # 全书学习：书本列表
│   ├── TextbookCreateActivity.kt        # 新建书本（书名/出版社/版次 或 拍照大纲 → AI 生成目录）
│   ├── TextbookDetailActivity.kt        # 书本目录（大章 → 小章）
│   ├── SectionStudyActivity.kt          # 小章学习（AI 三模块：知识点 / 公式 / 例题）
│   ├── WordListActivity.kt              # 我的词库（已学单词列表）
│   ├── AiSolveActivity.kt               # AI 识图解题（拍照 / 选图 / 文字 + 裁剪 + 增强 + 自定义提示词）
│   ├── CropActivity.kt                  # 图片裁剪（拖拽四角 + 旋转）
│   ├── StatsActivity.kt                 # 学习统计（概览 / 7 天图表 / 日历 / 今日内容）
│   ├── SettingsActivity.kt              # 设置中心（目标/提醒/外观/更新/数据）
│   ├── adapter/WordListAdapter.kt
│   ├── adapter/DictionaryAdapter.kt      # 词典列表（单词 + 音标 + 释义 + 未学/需复习/已掌握标签）
│   ├── ai/DeepSeekClient.kt             # DeepSeek 对话补全（文本 + 图片 base64）
│   ├── base/BaseActivity.kt             # 套用界面风格 + 自定义背景
│   ├── view/CropView.kt                 # 自绘裁剪视图
│   ├── data/
│   │   ├── WordRepository.kt            # 词库流式解析（JsonReader，异步 + 缓存）
│   │   ├── ProgressStore.kt             # 学习进度持久化
│   │   ├── SettingsStore.kt             # 应用设置（风格/背景/更新/提醒/AI Key）
│   │   └── TextbookStore.kt             # 书本目录与小章生成内容（filesDir JSON）
│   ├── model/Word.kt                    # Word / Familiarity / LearnRecord
│   ├── model/Textbook.kt                # Textbook / Chapter / Section / SectionContent / StudyStyle
│   ├── update/
│   │   ├── UpdateChecker.kt             # 直连 GitHub Release 检查版本
│   │   ├── ApkDownloader.kt             # 下载 APK + FileProvider 安装
│   │   └── UpdateUi.kt                  # 更新弹窗 / 下载进度
│   ├── util/
│   │   ├── ThemeStyle.kt                # 5 套界面风格
│   │   ├── BackgroundHelper.kt          # 背景图解码（降采样）+ 蒙版
│   │   ├── ImageEnhancer.kt             # 图像增强（灰度/对比度/高斯锐化/Otsu 二值化）
│   │   ├── GoalEditor.kt                # 目标日 + 手写目标语句编辑弹窗
│   │   ├── GoalCountEditor.kt           # 每日练词数量编辑弹窗（自定义 1–500 + 常用数量）
│   │   ├── AiResultDialog.kt            # AI 结果弹窗（翻译/造句，可一键复制）
│   │   ├── NotificationHelper.kt        # 通知渠道
│   │   └── ReminderNotifier.kt          # 提醒通知构建（定时/测试共用）
│   └── work/
│       ├── ReminderScheduler.kt         # AlarmManager 每日闹钟调度
│       ├── ReminderReceiver.kt          # 闹钟触发 → 发通知 + 续期
│       └── BootReceiver.kt              # 开机/更新后恢复闹钟
└── res/                                 # 布局、5 套主题（含 values-night）、图标、字符串
```

## AI 识图解题使用说明

1. 到 [platform.deepseek.com](https://platform.deepseek.com/) 申请 API Key（APP **不内置**任何 Key，Key 只保存在本机）
2. 首页 →「AI 识图解题」→ 填写 API Key
3. 三种出题方式（可组合）：
   - **拍照**：直接拍题目照片
   - **相册**：选择已有题目图片
   - **文字**：手动输入题目（也可与图片同时提交）
4. **提示词由你自己写**（已内置一份默认提示词，可随意修改；会随题目一起发送）
5. 点「开始解题」→ 显示解答，可一键复制

> 默认模型 `deepseek-flash` 支持图片理解（OpenAI 兼容格式，图片以 base64 内联发送，最长边自动压缩到 1600px）。
> 也可在界面里改成其它模型名。图片使用限制参考 [DeepSeek 图像理解文档](https://api-docs.deepseek.com/zh-cn/guides/vision/)（支持 JPEG/PNG/GIF/WebP，单图 ≤32MiB）。

## 技术要点

- **Kotlin + Material 3 + ViewBinding**，minSdk 26 / targetSdk 34
- 构建：AGP 9.3.0（**使用内置 Kotlin，不应用 KGP 插件**）+ Gradle 9.7.1 + JDK 17
- 进度持久化：SharedPreferences 存 JSON；「今日已学」按日期自动归零（跨天重置）
- 词库：`JsonReader` 流式解析 1.4MB / 6662 词，IO 线程 + 单次缓存 + 冷启动预载
- 学习流程：队列制；不认识的词重新入队，`counted` 标记避免重复计入今日进度
- 主题：`values` + `values-night` 双套配色；图标全部为 vector drawable（无 PNG 资源）

## 后续规划（未实现）

- 学习统计页（每日曲线、记忆曲线）
- 单词发音（TTS）
- 艾宾浩斯间隔复习（当前为「需复习词连续答对 2 次掌握」的手动复习）
- 词库扩充 / 导入自定义词库

## 版本

| 版本 | 内容 |
| --- | --- |
| v1.0 | 基础 UI（首页 / 我的词库 / 设置）+ 六级单词每日学习功能（示例词库 103 词） |
| v1.1 | **完整六级词库 6662 词**（含四级基础，97% 带例句）；词库改为流式解析 + 异步加载 + 冷启动预载；无例句词条自动隐藏例句区 |
| v1.2 | **界面风格自选**（5 套配色，含深色适配）；**自定义背景图**（选图 + 淡化程度 + 恢复默认）；**应用内更新**（直连 GitHub Release 检查 → 下载 → 安装，支持启动自动检查）；**每日通知提醒**（自定义时间 + 进度提示 + 点击进学习页） |
| v1.3 | **AI 识图解题**（拍照/相册/文字 + 用户自备 DeepSeek Key 与自定义提示词，`deepseek-flash` 图片理解）；**提醒可靠性修复**：WorkManager 改为 AlarmManager + 开机自动恢复闹钟，新增「测试提醒」与后台运行排障入口；通知渠道升级为高重要性（横幅+提示音）；移除 WorkManager 依赖，权限更精简 |
| v1.4 | **图片裁剪与增强**：自绘裁剪视图（拖拽四角 + 旋转 90°）、图像增强（灰度化 / 自动对比度 / **高斯反锐化掩模** / Otsu 二值化），让题目照片文字更清晰；**学习统计**：今日/连续/累计/认识率概览 + 近 7 天柱状图 + 今日学习内容列表；**学习日历**：翻月浏览、标注每日学习量与达标状态、**点击日期自定义该日目标**（跟随默认 / 休息日 / 5·10·20·30·50 词），学习页与首页按日目标联动 |
| v1.5 | **目标日 + 手写目标语句**：日历选一天写下目标/激励语句即设为目标日（日历中标出目标日）；**首页最底部**每次打开显示目标语句与**距离天数倒计时**（今天 / 已过 N 天分别提示）；首页点目标卡片可直接改日期与内容，支持清除目标 |
| v1.5.1 | **修复裁剪页全黑**：`CropView.setBitmap()` 未主动计算图片显示区域（设置图片不改变 View 尺寸，`onSizeChanged` 不回调），导致裁剪区空白；现在设置/旋转后立即重新适配，并在 `onDraw` 增加兜底。**修复预览看不清全图**：AI 解题页预览由 `centerCrop`（裁切放大）改为 `fitCenter`（完整显示全图）并加高到 240dp；**新增点击图片全屏查看**细节 |
| v1.6 | **六级学习合并页**：今日进度 + 统计概览 + 我的词库合并为一个页面（首页原「今日进度卡」与「我的词库」入口整合进来）。**全书学习（新）**：输入书名/出版社/版次或拍照上传目录页 → AI 生成**全书目录**（大章→小章）→ 点小章「开始学习」→ AI 输出**知识点 / 公式 / 例题**三模块并分框展示；**语言风格可选**（直白易懂 / 严谨全面），生成结果本地缓存、可复制、可清除 |
| v1.7 | **多图上传**：AI 解题与全书目录都支持**最多 6 张**图片（相册多选 + 缩略图选择，可单张裁剪/增强）；**API 设置集中到设置页**（Key + 模型 + 保存按钮，各页面只显示接口状态）；**用量与计费**：累计/今日请求数、输入输出 token、按可自定义单价估算费用、一键重置；**峰谷提醒**：实时峰/谷状态与切换倒计时，可开启每天 00:30「谷时开始」通知（AlarmManager 调度） |
| v1.71 | **自定义提醒句子**：设置 → 每日提醒 可写下自己的提醒语（如「该背单词啦，坚持就是胜利」），通知中显示该句子 + 今日进度，留空则只显示进度；**账户余额显示**：设置 → AI 接口新增「账户余额」，点击或进入页面自动查询 `GET /user/balance`，显示总额与赠送/充值明细，余额不足时告警 |
| v1.72 | **修正高峰/空闲时段（重要）**：按官方最新定价页，**高峰时段 = 北京时间周一至周五 9:00-12:00、14:00-18:00，其余全部为空闲时段（约 5 折）**；相应更新状态文案、下次切换倒计时、「空闲时段开始提醒」（改为在最近一个高峰结束点 12:00 / 18:00 通知）与计费说明 |
| v1.73 | **模型可自选**：设置 → AI 接口 的模型改为**下拉选择**（也可手动输入），点击「刷新可用模型」调用 `GET /models` 拉取账号可用模型并缓存；选择模型时**自动套用该模型的高峰默认单价**（flash 输入¥2/输出¥8、v4-pro 输入¥9/输出¥27），并提示 flash 支持图片理解、v4-pro 仅文本 |
| v1.80 | **（即 v1.8；上一版是 1.73，版本号按段号递增必须写 1.80，写成 1.8 会导致老版本 App 认为 8 < 73 而检测不到更新）** **每日练词数量自定义**：设置 → 每日学习目标改为**手输任意数量 1–500**（含 10/20/30/50/100 快捷键），学习日历增加「自定义数量…」。**独立复习**：六级学习页显示「复习需复习的 N 个词」，一键过一遍所有需复习词——先只给单词回忆，点「想不起来」才显示释义例句；**连续答对 2 次才算掌握并自动摘掉标记**，答错清零并排到队尾，顶部显示「已掌握 x / N · 剩余 y 个」。**词典**：六级学习页内嵌 6662 词全览 + **页内搜索**（英文模糊 / 中文释义 / 词性），每词带未学·需复习·已掌握标签，详情弹窗可 **AI 造句 / 英译中 / 标记需复习或已掌握**。**AI 翻译 / 造句**：输入中英文即可**中英互译**（自动判断方向）或 **AI 造句**（3 个六级例句 + 翻译 + 搭配提示），结果可一键复制并显示 token 用量。以上功能**全部集中在「六级学习」页**，不新增页面 |
